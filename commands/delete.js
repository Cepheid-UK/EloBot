/* Debug command for deleting all database entries */

exports.run = async (client, message, args, database) => {
    //database.query({sql: `DELETE FROM active_games WHERE player1='${message.author.tag}';`})
    //database.query({sql: `DELETE FROM open_challenges WHERE discord_id='${message.author.tag}';`})
    //message.channel.send(`all active games and open challenges have been deleted`)
}